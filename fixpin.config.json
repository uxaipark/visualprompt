{
  "$comment": "VisualPrompt target config. Set a target mode to load it quickly from the toolbar.",
  "targets": [
    {
      "id": "local-dev",
      "label": "Local dev server",
      "mode": "local",
      "url": "http://localhost:3000",
      "$comment_repoRoot": "The actual source repo root for this frontend. Used to add file path hints to the fixpoint.",
      "repoRoot": "./demo-app"
    }
  ],
  "$comment_default": "Default target id to show when starting without a URL (optional)",
  "defaultTarget": null
}
