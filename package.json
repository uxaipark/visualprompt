{
  "name": "visualprompt",
  "version": "0.1.0",
  "description": "VisualPrompt — pin edit prompts onto a web UI to collect fixpoints, drop them into the server's inbox directory, and let an agent edit the actual source",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "node --watch server/index.js",
    "dev:client": "vite",
    "build": "vite build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "preview": "npm run build && npm run start"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.17.0",
    "cheerio": "^1.0.0",
    "express": "^4.19.2",
    "playwright": "^1.61.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^9.0.1",
    "cross-env": "^7.0.3",
    "vite": "^6.0.0"
  }
}
